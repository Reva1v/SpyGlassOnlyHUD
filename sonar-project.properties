sonar.projectKey=Reva1v_SpyGlassOnlyHUD
sonar.organization=reva1v

sonar.sources=src/main/java

# Vendored compile-only stubs of the Mod Menu API. Their signatures must mirror
# upstream (com.terraformersmc.modmenu) byte for byte, so findings there are not
# actionable for us.
#
# NOTE: SonarCloud Automatic Analysis ignores this file, so the same exclusion is
# also set in the project settings (Administration -> Analysis Scope). Keep the
# two in sync; this file is what a CI-based scanner would use.
sonar.exclusions=src/main/java/com/terraformersmc/**
